# setup.py
from setuptools import setup, find_packages

setup(
    name="waitlib",
    version="0.1",
    packages=find_packages(),
    author="Your Name",
    description="Simple wait/delay library for Python",
    python_requires=">=3.6",
)
